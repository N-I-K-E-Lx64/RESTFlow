package com.restflow.core.WorkflowExecution.Condition;

public enum EConditionType {

    SMALLER,
    BIGGER,
    NUMBER_EQUALS,
    NOT_NUMBER_EQUALS,
    STRING_EQUALS,
    NOT_STRING_EQUALS,
    STRING_CONTAINS,
    NOT_STRING_CONTAINS
}
